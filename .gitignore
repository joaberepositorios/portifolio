node_modules
dist
*.local

# PDFs pessoais (certificados com CPF etc.) não vão para o repositório nem para o site
*.pdf

# Gerado por `npm run build` (é o mesmo arquivo que vai para o branch gh-pages)
Portfolio.html
